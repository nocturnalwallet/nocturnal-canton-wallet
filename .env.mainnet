# Loaded only by `yarn build:prod` (wxt build --mode mainnet).
# Restricts the wallet to Mainnet only (see lib/network.ts IS_MAINNET_ONLY_BUILD).
# Merged on top of the base .env, so real secrets still live in .env (gitignored).
VITE_MAINNET_ONLY=true
